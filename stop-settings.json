{
  "auto-resume": false,
  "_comment": "Auto-resume opt-in for the context-handoff POC build. Set to false: every slice in the design notes is built and tested, so the next stopping point is a real one."
}
